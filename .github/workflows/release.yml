name: Release

on:
  push:
    tags:
      - 'v*'

permissions:
  contents: read
  packages: write

jobs:
  build-and-push:
    name: build-and-push
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: docker/setup-buildx-action@v3

      - uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - id: meta
        uses: docker/metadata-action@v5
        with:
          images: ghcr.io/alternet-dev/wss-mux
          tags: |
            type=ref,event=tag
            type=raw,value=latest,enable=${{ !contains(github.ref, '-') }}
          labels: |
            org.opencontainers.image.source=https://github.com/alternet-dev/wss-mux
            org.opencontainers.image.licenses=MIT OR Apache-2.0
            org.opencontainers.image.description=WebSocket multiplexer for server-driven event fanout

      # linux/amd64 only. arm64 via QEMU emulation on x86 runners is ~20× the
      # native compile time (40+ min for a Rust release build). When/if arm64
      # demand materializes, switch to cargo-zigbuild or a matrix of native
      # arm64 runners — not back to QEMU.
      - uses: docker/build-push-action@v6
        with:
          context: .
          platforms: linux/amd64
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          cache-from: type=gha
          cache-to: type=gha,mode=max
